import { describe, it, expect } from 'vitest';

describe('ExerciseStats component', () => {
  it('should render exercise stats list', () => {
    expect(true).toBe(true);
  });

  it('should display frequency per exercise', () => {
    expect(true).toBe(true);
  });

  it('should sort by frequency', () => {
    expect(true).toBe(true);
  });
});