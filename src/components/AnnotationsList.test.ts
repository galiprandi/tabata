import { describe, it, expect } from 'vitest';

describe('AnnotationsList component', () => {
  it('should render annotations list', () => {
    expect(true).toBe(true);
  });

  it('should handle search', () => {
    expect(true).toBe(true);
  });
});